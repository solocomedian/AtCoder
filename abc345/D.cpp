#include <algorithm>
#include <cmath>
#include <iostream>
#include <map>
#include <numeric>
#include <string>
#include <vector>
using namespace std;

int main()
{
  int                 n, h, w;
  vector<vector<int>> v(n, vector<int>(2));
  cin >> n >> h >> w;
  for (int i = 0; i < n; i++) {
    int a, b;
    cin >> v[i][0] >> v[i][1];
  }
}